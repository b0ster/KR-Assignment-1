class SatSolver:

    def __init__(self, heuristics=None) -> None:
        self.heuristics = heuristics
        pass

    def solve(self, rules, problem):
        # if self.heuristics is not None:
        #   todo, do something with heuristics if they are available
        # todo, do some reasoning with the rules and return a DIMAC solution
        return ""  # must be a DIMAC string with the solution
